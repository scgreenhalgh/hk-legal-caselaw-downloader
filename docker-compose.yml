# Auto-generated — do not edit by hand.
# To update: edit VPN_REGION_* in .env, then run:
#   uv run python scripts/generate_compose.py
#
# Usage:
#   docker compose up -d         # start all VPNs
#   docker compose up -d vpn-1   # start just one for testing
#   uv run python scripts/test_vpn.py   # verify connectivity + DNS
#
# To use WireGuard instead of OpenVPN, see the README or generate configs
# with PIA's manual-connections scripts (github.com/pia-foss/manual-connections).

x-vpn-common: &vpn-common
  image: qmcgaw/gluetun:latest
  cap_add:
    - NET_ADMIN
  devices:
    - /dev/net/tun:/dev/net/tun
  healthcheck:
    test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9999 || exit 1"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 30s
  restart: unless-stopped

x-vpn-env: &vpn-env
  VPN_SERVICE_PROVIDER: private internet access
  VPN_TYPE: openvpn
  OPENVPN_USER: ${PIA_USER}
  OPENVPN_PASSWORD: ${PIA_PASS}
  HTTPPROXY: "on"
  HTTPPROXY_LISTENING_ADDRESS: ":8888"
  HTTPPROXY_STEALTH: "on"

services:
  vpn-1:
    <<: *vpn-common
    container_name: hklii-vpn-1
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_1}
    ports:
      - "8888:8888"

  vpn-2:
    <<: *vpn-common
    container_name: hklii-vpn-2
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_2}
    ports:
      - "8889:8888"

  vpn-3:
    <<: *vpn-common
    container_name: hklii-vpn-3
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_3}
    ports:
      - "8890:8888"

  vpn-4:
    <<: *vpn-common
    container_name: hklii-vpn-4
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_4}
    ports:
      - "8891:8888"

  vpn-5:
    <<: *vpn-common
    container_name: hklii-vpn-5
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_5}
    ports:
      - "8892:8888"

  vpn-6:
    <<: *vpn-common
    container_name: hklii-vpn-6
    environment:
      <<: *vpn-env
      SERVER_REGIONS: ${VPN_REGION_6}
    ports:
      - "8893:8888"
