"""Stub — real implementation lands in the next commit."""


def fetch_gluetun_servers() -> str:
    return ""


def parse_servers(markdown: str) -> list[dict]:
    return []


def pick_servers_per_region(servers, regions, per_region):
    return []


def render_compose(picked_servers) -> str:
    return ""
